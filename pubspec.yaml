name: prograde_license_validator
description: Prograde license validator (wire contract impl) for NavMath/HudSim.
version: 0.1.0
publish_to: none
environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"
dependencies:
  flutter:
    sdk: flutter
  cryptography: ^2.7.0
  http: ^1.2.0
  crypto: ^3.0.3
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.3.0
  device_info_plus: ^10.1.0
  package_info_plus: ^8.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
